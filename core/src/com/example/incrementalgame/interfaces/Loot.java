package com.example.incrementalgame.interfaces;

import com.example.incrementalgame.managers.ResourceManager;

public interface Loot {
    public void apply(ResourceManager resourceManager); // Apply loot to the ResourceManager
}
